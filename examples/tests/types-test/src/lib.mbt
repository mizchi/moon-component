// Component entry point - Generated by wit-bindgen-moonbit
// This file re-exports FFI functions for wasm linking

///| Re-export cabi_realloc for wasm linking
pub fn cabi_realloc(
  old_ptr : Int,
  old_size : Int,
  align : Int,
  new_size : Int,
) -> Int {
  @cabi.cabi_realloc(old_ptr, old_size, align, new_size)
}

///| Re-export for wasm linking
pub fn wasmExportEchoBool(val : Bool) -> Bool {
  @exports.wasmExportEchoBool(val)
}

///| Re-export for wasm linking
pub fn wasmExportEchoF32(val : Float) -> Float {
  @exports.wasmExportEchoF32(val)
}

///| Re-export for wasm linking
pub fn wasmExportEchoF64(val : Double) -> Double {
  @exports.wasmExportEchoF64(val)
}

///| Re-export for wasm linking
pub fn wasmExportEchoS32(val : Int) -> Int {
  @exports.wasmExportEchoS32(val)
}

///| Re-export for wasm linking
pub fn wasmExportEchoS64(val : Int64) -> Int64 {
  @exports.wasmExportEchoS64(val)
}

///| Re-export for wasm linking
pub fn wasmExportEchoString(val_ptr : Int, val_len : Int) -> Int {
  @exports.wasmExportEchoString(val_ptr, val_len)
}

///| Re-export for wasm linking
pub fn wasmExportColorName(c : Int) -> Int {
  @exports1.wasmExportColorName(c)
}

///| Re-export for wasm linking
pub fn wasmExportEchoColor(c : Int) -> Int {
  @exports1.wasmExportEchoColor(c)
}

///| Re-export for wasm linking
pub fn wasmExportEchoPermissions(p : Int) -> Int {
  @exports2.wasmExportEchoPermissions(p)
}

///| Re-export for wasm linking
pub fn wasmExportHasRead(p : Int) -> Bool {
  @exports2.wasmExportHasRead(p)
}

///| Re-export for wasm linking
pub fn wasmExportHasWrite(p : Int) -> Bool {
  @exports2.wasmExportHasWrite(p)
}

///| Re-export for wasm linking
pub fn wasmExportCountList(vals_ptr : Int, vals_len : Int) -> Int {
  @exports3.wasmExportCountList(vals_ptr, vals_len)
}

///| Re-export for wasm linking
pub fn wasmExportDivide(a : Int, b : Int) -> Int {
  @exports3.wasmExportDivide(a, b)
}

///| Re-export for wasm linking
pub fn wasmExportEchoListS64(vals_ptr : Int, vals_len : Int) -> Int {
  @exports3.wasmExportEchoListS64(vals_ptr, vals_len)
}

///| Re-export for wasm linking
pub fn wasmExportSumList(vals_ptr : Int, vals_len : Int) -> Int {
  @exports3.wasmExportSumList(vals_ptr, vals_len)
}

///| Re-export for wasm linking
pub fn wasmExportAdd2(a : Int, b : Int) -> Int {
  @exports4.wasmExportAdd2(a, b)
}

///| Re-export for wasm linking
pub fn wasmExportAdd3(a : Int, b : Int, c : Int) -> Int {
  @exports4.wasmExportAdd3(a, b, c)
}

///| Re-export for wasm linking
pub fn wasmExportAdd4(a : Int, b : Int, c : Int, d : Int) -> Int {
  @exports4.wasmExportAdd4(a, b, c, d)
}

///| Re-export for wasm linking
pub fn wasmExportConcat3(a_ptr : Int, a_len : Int, b_ptr : Int, b_len : Int, c_ptr : Int, c_len : Int) -> Int {
  @exports4.wasmExportConcat3(a_ptr, a_len, b_ptr, b_len, c_ptr, c_len)
}

///| Re-export for wasm linking
pub fn wasmExportMixedParams(n : Int, s_ptr : Int, s_len : Int, b : Bool) -> Int {
  @exports4.wasmExportMixedParams(n, s_ptr, s_len, b)
}

///| Re-export for wasm linking
pub fn wasmExportNoParamsNoReturn() -> Unit {
  @exports5.wasmExportNoParamsNoReturn()
}

///| Re-export for wasm linking
pub fn wasmExportNoReturn(msg_ptr : Int, msg_len : Int) -> Unit {
  @exports5.wasmExportNoReturn(msg_ptr, msg_len)
}

///|
fn main {
  ()
}
