// Component entry point - Generated by wit-bindgen-moonbit
// This file re-exports FFI functions for wasm linking

///| Re-export cabi_realloc for wasm linking
pub fn cabi_realloc(
  old_ptr : Int,
  old_size : Int,
  align : Int,
  new_size : Int,
) -> Int {
  @cabi.cabi_realloc(old_ptr, old_size, align, new_size)
}

///| Re-export for wasm linking
pub fn wasmExportBlobNew(data_ptr : Int, data_len : Int) -> Int {
  @exports.wasmExportBlobNew(data_ptr, data_len)
}

///| Re-export for wasm linking
pub fn wasmExportBlobSize(handle : Int) -> Int {
  @exports.wasmExportBlobSize(handle)
}

///| Re-export for wasm linking
pub fn wasmExportBlobRead(handle : Int, offset : Int, len : Int) -> Int {
  @exports.wasmExportBlobRead(handle, offset, len)
}

///| Re-export for wasm linking
pub fn wasmExportBlobWrite(handle : Int, offset : Int, data_ptr : Int, data_len : Int) -> Unit {
  @exports.wasmExportBlobWrite(handle, offset, data_ptr, data_len)
}

///| Re-export for wasm linking
pub fn wasmExportCreateBlob(data_ptr : Int, data_len : Int) -> Int {
  @exports.wasmExportCreateBlob(data_ptr, data_len)
}

///| Re-export for wasm linking
pub fn wasmExportGetBlobSize(b : Int) -> Int {
  @exports.wasmExportGetBlobSize(b)
}

///| Re-export for wasm linking
pub fn wasmExportConsumeBlob(b : Int) -> Int {
  @exports.wasmExportConsumeBlob(b)
}

///|
fn main {
  ()
}
